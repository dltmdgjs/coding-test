int mx = max(a.size(), b.size());

    // for (int i=0; i<mx; i++) {
    //     int sum = olim;
    //     if (i >= a.size()) {
    //         sum += b[i];
    //     } else if (i >= b.size()) {
    //         sum += a[i];
    //     } else {
    //         sum += a[i] + b[i];
    //     }
    //     olim = sum/10;
    //     r.push_back(sum%10);
    // }

    // for (int i=r.size()-1; i>=0; i--) {
    //     cout << r[i];
    // }
